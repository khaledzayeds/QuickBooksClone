// Shared price mode for QuickBooks-style transaction line grids.
// Kept separate to avoid import cycles between the public wrapper and grid implementation.

enum TransactionLinePriceMode { purchase, sales }
